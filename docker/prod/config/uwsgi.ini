[uwsgi]
# The base directory (full path)
chdir = /home/web

# Django's wsgi file
module = KakaoAnalysis.wsgi:application
master = true

# Maximum number of worker processes
processes = %(%k * 3)

# The socket (use the full path to be safe
socket = :3031
chmod-socket = 660
pidfile = /tmp/expression.pid

# Clear environment on exit
vacuum = true
max-requests = 5000

# For lazy apps
lazy-apps = true

# For buffer size
buffer-size = 65536